#include "main.h"
#include <stdio.h>

/**
 * _puts - Prints a string to the standard output followed by a new line.
 * @str: The string to be printed.
 */
void _puts(char *str);

/**
 * main - Entry point for the program.
 *
 * This function serves as the entry point for the program.
 * It prints a message and returns 0 to indicate success.
 *
 * Return: 0 on success.
 */
int main(void)
{
	_puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
